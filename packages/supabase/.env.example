# Example env file for packages/supabase — used by the local Supabase CLI and referenced from config.toml
# Copy this to .env when running `supabase start` locally and fill the values.
# DO NOT commit real secrets. Use your platform's secret manager for production.

# Optional: OpenAI key used by Supabase Studio AI features
# (config.toml: openai_api_key = "env(OPENAI_API_KEY)")
OPENAI_API_KEY=

# Optional: S3-compatible object storage settings (used by experimental features / OrioleDB S3)
# Set these if you want Supabase to talk to an S3-compatible endpoint for storage or backups.
# S3_HOST should be a fully qualified host (no scheme), e.g. "my-bucket.s3.us-east-1.amazonaws.com"
S3_HOST=
S3_REGION=
S3_ACCESS_KEY=
S3_SECRET_KEY=

# Optional: SMTP / email provider secrets for auth.email.smtp
# If you enable auth.email.smtp in config.toml, set these (example uses SendGrid api key pattern)
SENDGRID_API_KEY=
# Or generic SMTP settings (uncomment in config.toml to use)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
ADMIN_EMAIL=
SENDER_NAME=

# Optional: Twilio auth token used by auth.sms.twilio configuration
# (config.toml references env(SUPABASE_AUTH_SMS_TWILIO_AUTH_TOKEN))
SUPABASE_AUTH_SMS_TWILIO_AUTH_TOKEN=

# Notes:
# - Keep server-only secrets out of client bundles. Do not expose any PRIVATE keys to the browser.
# - The Next.js app in this repo expects its own `.env.local` under packages/nextjs/ with NEXT_PUBLIC_* vars.
# - After editing this file, run `supabase start` from the `packages/supabase` folder (or use `yarn supabase:start` at repo root).
